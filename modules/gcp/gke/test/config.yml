# -----------------------------------------------------------------------------
# File: ctlabs-terraform/gke/test/config.yml
# -----------------------------------------------------------------------------

gke:
  project: ctlabs-prj-2025101601
  location: us-central1-a
  name: dev-cluster-01
  network: default
  subnetwork: default
  node_pools:
    - name: system-pool
      machine_type: e2-medium
      node_count: 1
      disk_size_gb: 20
    - name: apps-pool
      machine_type: e2-standard-2
      node_count: 2
      disk_size_gb: 50
